import 'package:flutter/material.dart';

class AppColors {
  // Blue gradient theme colors to match Figma
  static const Color primaryBlue = Color(0xFF1E3A8A);
  static const Color secondaryBlue = Color(0xFF3B82F6);
  static const Color darkBlue = Color(0xFF1E40AF);
  static const Color lightBlue = Color(0xFF60A5FA);

  // Background gradient colors
  static const Color gradientStart = Color(0xFF1E3A8A);
  static const Color gradientEnd = Color(0xFF3B82F6);

  // Card colors
  static const Color cardBackground = Color(0xFF2563EB);
  static const Color cardBackgroundDark = Color(0xFF1D4ED8);

  // Accent colors
  static const Color yellowAccent = Color(0xFFFBBF24);
  static const Color orangeAccent = Color(0xFFF97316);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFE5E7EB);
  static const Color textTertiary = Color(0xFFD1D5DB);

  // Timer colors
  static const Color timerBackground = Color(0xFFFFFFFF);
  static const Color timerText = Color(0xFF1F2937);

  // Status colors
  static const Color favoriteColor = Color(0xFFFBBF24);
  static const Color completedColor = Color(0xFF10B981);

  // Button colors
  static const Color buttonBackground = Color(0xFF3B82F6);
  static const Color buttonText = Color(0xFFFFFFFF);

  // Border colors
  static const Color outline = Color(0xFF60A5FA);
  static const Color outlineVariant = Color(0xFF93C5FD);
}
